<!-- SimplyTabby
		
<%= SimplyTabby.display_system_information[:public].map { |k, v| "#{k}: #{v}" }.join("\n") %>
mongrel_port: <%= Rails::MONGREL_PORT %>
		
<%= SimplyTabby.display_crypt_system_information %>
	
-->
